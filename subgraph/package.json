{
  "name": "weather-oracle-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "cross-env-shell \"graph auth --product hosted-service $GRAPH_DEPLOY_KEY\"",
    "deploy": "cross-env-shell \"graph deploy --product hosted-service $SUBGRAPH_SLUG\""
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.79.0",
    "@graphprotocol/graph-ts": "^0.36.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.6.3"
  }
}