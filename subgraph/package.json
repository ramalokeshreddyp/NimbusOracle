{
  "name": "weather-oracle-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service $GRAPH_DEPLOY_KEY",
    "deploy": "graph deploy --product hosted-service $SUBGRAPH_SLUG"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.79.0",
    "@graphprotocol/graph-ts": "^0.36.0",
    "typescript": "^5.6.3"
  }
}